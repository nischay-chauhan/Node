{
  "name": "purenode",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "tsc && node dist/index.js",
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'tsc && node dist/index.js'",
    "build": "tsc",
    "test": "tsc && node --test dist/tests/**/*.test.js",
    "test:watch": "nodemon --watch 'src/**/*.ts' --watch 'tests/**/*.ts' --exec 'tsc && node --test dist/tests/**/*.test.js'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.14.2",
    "nodemon": "^3.1.3",
    "typescript": "^5.4.5"
  }
}
